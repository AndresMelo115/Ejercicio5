@model IEnumerable<Pokedex.Models.Tipos>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreT)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreT)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TiposId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TiposId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TiposId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
